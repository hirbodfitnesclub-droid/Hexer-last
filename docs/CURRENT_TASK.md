# CURRENT_TASK.md — اشاره‌گر فاز جاری

> **فاز E (کارت‌به‌کارت + رسید) کامل و تثبیت شده است** (تسک‌های C1–C5: میگریشن `28_card_to_card_system.sql`، تریگر تلگرام `30_telegram_notifications.sql`، `billingService`، و مودال‌های `SubscriptionModal`/`PaymentMethodModal`/`ReceiptUploadModal`).

## فاز جاری: F — رفع باگ، بهبود UX، PWA و فیچرهای جدید
نقشه‌ی کامل و تسک‌های اجرایی در **`tasks.md` (F1–F9)** است. مرجع معماری: **`PROJECT.md §۸`** و **`ARCHITECTURE.md §۹`**.

### ترتیب پیشنهادی اجرا
- مستقل (هر زمان): **F1** (PWA + سافاری)، **F2** (جهت آیکون RTL)، **F3** (اسکرول افقی + مصرف در اشتراک)، **F4** (دکمه‌های حالت AI + مصرف در چت)، **F5** (جایگاه دکمه‌ی لینک)، **F6** (فید لبه‌ها).
- متوالی: **F7 → F8** (RAG پروژه‌ها: ابتدا اسکیما/وکتورایز، سپس زمینه/Intent).
- مستقل: **F9** (سیستم تیکت پشتیبانی).

### یادآوری قوانین حیاتی
- هیچ فایل SQL موجود ویرایش نمی‌شود؛ فایل جدید با پیشوند `31`+ ساخته شود (اجرای دستی توسط مالک).
- ضدالگوهای `PROJECT.md §۴/§۶/§۸` (به‌ویژه ۳۳–۴۰) و سلسله‌مراتب z-index و الگوی مودال موبایل `ARCHITECTURE.md §۷` بی‌چون‌وچرا رعایت شوند.
- هر تغییر باید رفتار موجود را رگرسیون ندهد و در موبایل روان، بدون بیرون‌زدگی عرضی و بدون کرش باشد.
